# Minimum Supported Rust Version
msrv = "1.93.0"

# Warn on wildcard imports
warn-on-all-wildcard-imports = true

# Disallowed methods — prefer RetryPolicy for retry delays
disallowed-methods = [
    { path = "std::thread::sleep", reason = "use RetryPolicy for retry delays instead of blocking sleep" },
]
